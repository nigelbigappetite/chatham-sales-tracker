# Google Sheets API Key (for reading data)
# Get your API key from: https://console.cloud.google.com/apis/credentials
# See GOOGLE_SHEETS_SETUP.md for detailed instructions

VITE_GOOGLE_SHEETS_API_KEY=your_api_key_here

# Create Order / Mark fulfilled: Google Apps Script Web App URL
# 1. In your Google Sheet: Extensions → Apps Script, paste the code from scripts/AppendOrderToSheet.gs
# 2. Deploy as Web app (Execute as: Me, Who has access: Anyone), copy the URL (ends in /exec)
#
# Local dev: set below (VITE_ prefix so the app can call the script directly).
# VITE_APP_SCRIPT_WEB_APP_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
#
# Production (Vercel): set APP_SCRIPT_WEB_APP_URL in Vercel → Settings → Environment Variables
# (no VITE_ prefix). The app uses the /api/sheets-app proxy to avoid CORS.
# APP_SCRIPT_WEB_APP_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
